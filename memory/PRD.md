# StudioHub AI — PRD

## Problema / Visão
Plataforma SaaS premium para fotógrafos (React + FastAPI + MongoDB). Gestão completa: CRM, Sessões, Galerias, Contratos, Orçamentos, Financeiro, IA e Área do Cliente. Design minimalista/elegante, responsivo, dark/light, animações suaves. App em Português (PT/BR via i18n).

## Arquitetura
- `/app/backend/server.py`: FastAPI, MongoDB (motor async), rotas prefixadas `/api`.
- `/app/frontend`: React (CRA + Craco), Tailwind, Shadcn UI, `src/lib/api.js`, i18n em `src/lib/settings.jsx` e `src/lib/format.js`.
- Rota do módulo financeiro: `/financeiro` (App.js).

## Implementado
- MVP + Fase 1–7: Dashboard/CRM, Orçamentos, Contratos (assinatura digital), Assistente IA (Emergent LLM key), Galerias Premium, i18n PT/BR, Área do Cliente (JWT), melhorias de Galerias.
- **Fase 8 — Dashboard Financeiro** (2026): 5 KPIs (Receita do mês, Contas a receber, Contas a pagar, Lucro, Fluxo de caixa) + 2 gráficos (Receita mensal barras, Despesas por categoria donut). Endpoint `GET /api/finance/summary`.
- **Módulo Contas a Receber** (2026): CRUD + pagamento parcial + marcar pago; lista com cliente, sessão/projeto, total, recebido, saldo, vencimento, método, estado (Pendente/Parcial/Pago/Vencido); pesquisa, filtros, ordenação por vencimento. Integrado no KPI "Contas a receber". Rotas: `GET/POST /api/receivables`, `PUT /api/receivables/{id}`, `POST /api/receivables/{id}/payment`, `POST /api/receivables/{id}/pay`, `DELETE /api/receivables/{id}`. Testado (iteration_8.json) — sem regressões.
- **Módulo Contas a Pagar** (2026): CRUD + marcar pago; campos fornecedor, descrição, categoria (16), valor, vencimento, data pagamento, método, estado (Pendente/Pago/Vencido calculado/Cancelado), observações; pesquisa, filtro por estado, filtro por categoria, ordenação por vencimento; valores não-negativos. Rotas: `GET/POST /api/payables`, `GET /api/payables/categories`, `PUT /api/payables/{id}`, `POST /api/payables/{id}/pay`, `DELETE /api/payables/{id}`. Migração: coleção `expenses` substituída por `payables` como fonte única do KPI "Contas a pagar" (pendente+vencido) e do gráfico "Despesas por categoria" (pagas). PUT auto-preenche paid_date quando status→pago. Testado (iteration_9.json) — 136 passed, zero regressões.
- **Módulo Relatórios Financeiros** (2026): página `/financeiro/relatorios` (botão "Relatórios" em `/financeiro`). 6 análises: receita por mês, despesas por categoria, contas a receber por estado, contas a pagar por estado, fluxo de caixa (entradas/saídas/líquido) e top 10 clientes por faturação. Filtros: período (start/end), estado, categoria. Exportação PDF (reportlab) e Excel (openpyxl). Rotas: `GET /api/reports/financial`, `GET /api/reports/financial/export?format=xlsx|pdf`. Novas deps: openpyxl, reportlab. Testado (iteration_10.json) — 26/26 novos + 162 passed, zero regressões.
- **Módulo Loja Online — MVP** (2026): módulo independente em `/loja` (tabs Produtos/Categorias/Pedidos). Categorias: CRUD + pesquisa + ativo/inativo (`GET/POST/PUT /api/store/categories`, `PATCH /toggle`, `DELETE`). Produtos: CRUD + nome/descrição/categoria/preço/URL imagem/SKU/ativo + pesquisa + filtros (`GET /api/store/products` com ?category/?active, `POST/PUT/PATCH toggle/DELETE`; preço clampado ≥0). Pedidos: collection independente `store_orders` (isolada de `db.orders` da galeria) com estados Novo/Pago/Em Produção/Enviado/Entregue/Cancelado (`GET /api/store/orders`, `GET /states`, `POST`, `PUT`, `PATCH /status`, `DELETE`; número ENC-ANO-XXXX, total calculado). Carrinho: estrutura pronta (itens → pedido), SEM pagamentos/Stripe/uploads. Rota hardcoded antiga `/store/products` removida e unificada em `db.products`; galeria pública passa a usar `?active=true`. Frontend: Store.jsx + StoreProducts.jsx + StoreCategories.jsx + StoreOrders.jsx. Testado (iteration_12 e iteration_13.json) — 38/38 backend + frontend, zero regressões.
- **Loja Online — Fase 2** (2026): Galeria Pública (`/g/{access_token}`, ClientGallery.jsx) transformada em experiência de compra. Cada foto → "Adicionar produto" (grid-buy/lb-buy) abre modal (imagem, produto, quantidade, observações) → carrinho lateral (alterar qtd, remover, subtotal) → checkout (nome/email/telefone/notas, sem pagamento) → `POST /api/public/galleries/{token}/store-order` cria Pedido em `store_orders` com estado "Novo", nº ENC-ANO-XXXX, total calculado, fotos+produtos+telefone associados. Admin Loja→Pedidos: botão detalhe (view-order) mostra cliente, telefone, galeria, fotos compradas, produtos, observações e total. Backend: OrderItem +photo_name/photo_url/notes; Order/OrderCreate +customer_phone. Endpoint legacy `/order` (db.orders) preservado. Testado (iteration_14→15.json) — 19/19 backend + E2E, zero regressões.
- **Correção UX — Coordenação de painéis** (2026): novo `lib/panels.jsx` (PanelProvider + usePanels, estado global 'ai'|'cart'|'checkout'). Assistente IA agora fecha por botão X (ai-close), tecla ESC e clique fora (ai-backdrop). Apenas um painel lateral aberto de cada vez entre Assistente IA / Carrinho / Checkout (exclusão mútua). Comportamento idêntico desktop/mobile. Ficheiros: lib/panels.jsx (novo), components/AiAssistant.jsx, App.js (PanelProvider), pages/ClientGallery.jsx. Sem alterações de backend. Testado (iteration_16.json) — zero bugs, zero regressões.
- **Loja Online — Fase 3 (Pagamentos Stripe — MODO TESTE)** (Jun 2026): checkout real via Stripe Checkout Sessions (hosted) no fluxo de compra da galeria pública. Flow A (claimable sandbox) provisionado — chaves em `backend/.env` (STRIPE_SECRET_KEY/PUBLISHABLE_KEY/ACCOUNT_ID/WEBHOOK_SECRET/MODE), conta Stripe reivindicável pelo utilizador via onboarding_url. `POST /api/public/galleries/{token}/store-order` recalcula preços SERVER-SIDE a partir de `db.products` (anti-manipulação), cria a Checkout Session com metadata (order_id, order_number, gallery_id) e devolve checkout_url+session_id; grava no pedido stripe_session_id, payment_status='pending', currency, amount. `GET /api/public/checkout/status/{session_id}` é APENAS UI (não altera estado). `POST /api/stripe/webhook` (fonte única de verdade) valida assinatura e atualiza payment_status (paid/failed/refunded) + stripe_payment_intent_id + paid_at, SEM tocar no estado operacional. Frontend: ClientGallery redireciona para Stripe (botão "Pagar", debounce via `placing`); novas páginas `/payment/success` (polling) e `/payment/cancel` (PaymentResult.jsx). Modo tax: DIY (Stripe só processa; sem cálculo de imposto) — pode ativar cálculo de imposto mais tarde. Testado (iteration_20.json) — 6/6 backend + E2E completo (pagamento real com cartão teste → webhook → payment_status='paid', status operacional inalterado), zero regressões. NÃO deployed, NÃO guardado no GitHub (aguarda validação).
- **Fase 4 — Automação Pós-Venda** (Jul 2026): ao confirmar pagamento (webhook Stripe), automação idempotente `run_paid_automation`: cria receita no Financeiro (db.receivables, origem "Pedido Online", método Stripe, status pago), envia email ao cliente ("Pagamento recebido") e ao fotógrafo ("Novo pedido pago") via Resend gerido (EMERGENT_EMAIL_KEY), cria notificação `payment_received`, atividade CRM "Pedido X pago." e regista tudo no histórico cronológico do pedido. Estado operacional NÃO muda (Opção B). Novas coleções: notifications, activities. Endpoints: /api/notifications(+unread-count,/read,/read-all), /api/activities. Sino de notificações no Layout (NotificationBell.jsx) + KPIs no Dashboard (total_sales, paid_orders, avg_ticket, notifications_unread) + Atividade recente + histórico no detalhe do pedido. Testado (iteration_21.json) — 12/12 backend + E2E Stripe completo, zero regressões.
- **Fase 5.0 — Autenticação do Fotógrafo** (Jul 2026): auth JWT custom (bcrypt) para o painel de gestão. Coleções novas: `users`, `organizations`. Organização padrão "Studio E Fotografias" criada no startup + utilizador owner semeado (ADMIN_EMAIL/ADMIN_PASSWORD). Endpoints /api/auth/register|login|logout|me|forgot-password|reset-password (token reset scope="studio", 1h). Frontend: lib/auth.jsx (AuthProvider, token Bearer em localStorage `studio_token`), páginas StudioAuth.jsx (login/registo/forgot) e ResetPassword.jsx, ProtectedRoute em todas as rotas de gestão, perfil+logout no Layout (UserFooter). Interceptor em lib/api.js anexa `studio_token` (Portal usa portalApi próprio — sem conflito). NÃO implementado ainda: organization_id nas coleções existentes, filtros por org, planos, limites, billing, Admin SaaS (Fases 5.1+). Módulos existentes inalterados (sem filtro por org). Auto-testado: login/registo/forgot/reset/logout/rotas protegidas/persistência de sessão (curl + browser), galeria pública e Portal do Cliente sem regressão.
- **Fase 5.1 — Organization Base** (Jul 2026): coleção `organization_members` (relação utilizador↔organização com role, índice único {organization_id,user_id}). `ensure_membership()` idempotente. Registo de novo utilizador cria membership automaticamente; startup faz backfill (todos os utilizadores existentes ligados à org padrão "Studio E Fotografias"). NÃO alterado: queries existentes, CRM, Galerias, Loja, Financeiro, Stripe, Portal, Dashboard. NÃO implementado ainda: tenant isolation, organization_id nas coleções de negócio, planos, limites, billing, Admin SaaS. Testado: login/registo OK, 1 org, 3/3 users com membership, 0 sem org, endpoints existentes 200 — zero regressões.
- **Fase 5.4 — Planos e Limites (infraestrutura)** (Jul 2026): coleções `plans` (3 planos semeados: Starter/Pro/Business com limites {clients, galleries, storage_gb, users} e features {store, ai, automations}), `subscriptions` (org padrão → plano "business" ativo, semeada no startup) e `feature_flags` (overrides por org, materializa ao 1º flag). Helpers centrais: `get_org_plan`, `org_feature_enabled`, `org_limit`, `org_usage`, e as dependencies **`require_feature(name)`** (403) e **`require_limit(name)`** (402) — DEFINIDAS mas NÃO aplicadas a nenhum endpoint (não bloqueia nada nesta fase). Endpoints read-only: `GET /api/plans`, `GET /api/subscription` (plano+uso da org autenticada). Sem frontend, sem Stripe, sem bloqueio. Testado: /plans devolve 3 planos, /subscription mostra business+uso, endpoints existentes intactos (200).
- **Fase 5.2.1 — Catálogo público org-aware** (Jul 2026): fechada a nota conhecida da 5.2. Novo endpoint público `GET /api/public/galleries/{token}/products` que resolve a organização pela própria galeria (via token) e devolve APENAS os produtos ativos dessa organização. `GET /store/products` e `/store/categories` removidos da allowlist pública → agora exigem token (401 sem auth). Frontend `ClientGallery.jsx` passou a usar o endpoint por token. O frontend nunca envia organization_id. Testes automáticos atualizados (`test_tenant_isolation.py`, 9/9 ✅) incluindo `test_08_public_catalog_isolated` (galeria de A só mostra produtos de A; galeria de B só de B). Smoke test: galeria pública mostra catálogo correto + carrinho + checkout Stripe (€450) sem regressões. Já NÃO existe catálogo público misturado entre organizações.
- **Fase 5.2 — Tenant Isolation** (Jul 2026): helper central único `_TenantDB`/`_TColl` (wrapper de coleção) que injeta `organization_id` automaticamente em TODAS as leituras/escritas das coleções scoped, via `ContextVar current_org`. O org vem SEMPRE do token JWT (embebido em `create_studio_token`), definido pela dependency de router `tenant_context` (fail-closed: rotas de gestão sem token → 401). Público/portal (`/api/public/`, `/api/portal/`, `/api/auth/`) e catálogo GET (`/settings`, `/store/products`, `/store/categories`, `/templates`, `/store/orders/states`) permanecem sem scoping. Fluxos públicos de escrita (store-order, webhook/run_paid_automation, seed) definem `current_org` a partir do recurso (galeria/pedido/org padrão). Migração idempotente no startup: dados existentes → org padrão. Coleções scoped: clients, sessions, galleries, store_orders, products, store_categories, quotes, contracts, invoices, receivables, payables, events, notifications, activities, ai_messages, settings. Testes automáticos `backend/tests/test_tenant_isolation.py` (8/8 ✅): cria org B + user B, prova isolamento CRM/Galerias/Loja/Financeiro/Dashboard, 401 sem token, e bloqueio de leitura cross-org por id (404). Frontend NÃO alterado. Nota conhecida: catálogo GET /store/products é público (galeria pública sem login) — mostra produtos de todas as orgs até a galeria pública ser org-aware (fase futura, requer alteração de frontend).
- **Módulo IA — 4 Assistentes Especializados** (Jun 2026, PRIORIDADE 1): página `/ia` (IA.jsx) reformulada de chat único para 4 copilotos selecionáveis por cards, cada um com persona própria, ações rápidas e chat isolado por session_id. Assistentes: **Comercial** (propostas, orçamentos, emails, follow-up, WhatsApp), **Fotográfico** (roteiro de sessão, checklist de casamento, lista de equipamento, cronograma), **Financeiro** (previsão de faturação, fluxo de caixa, clientes em atraso, metas — usa dados REAIS: receivables/payables/invoices), **Marketing** (posts, copy Meta/Google Ads, legendas, emails de campanha). Backend: `POST /api/ai/assistant` (modelo AiAssistantIn: assistant/message/session_id), `ASSISTANT_PERSONAS`, `_assistant_context(assistant)` que injeta contexto de negócio conforme o tipo — tudo scoped por organization_id via `db` (_TenantDB). Modelo OpenAI gpt-5.4 via EMERGENT_LLM_KEY. Frontend: componente reutilizável `AssistantChat.jsx` (ações rápidas, botão copiar por resposta, markdown/whitespace). Gera conteúdo pronto a copiar (envio real de WhatsApp / publicação de anúncios = Fase 2 futura). Testado (iteration_22.json): backend 7/7 (4 personas 200 + dados reais no Financeiro), frontend 100% (4 abas + ações + envio custom), zero regressões.


## Backlog priorizado
- P1: Loja Online — Fase 3 (checkout real com Stripe) — ✅ CONCLUÍDO (modo teste, iteration_20). Falta: validação do utilizador + reivindicar conta Stripe (onboarding_url) antes de deploy.
- P1: Admin Panel (gestão de utilizadores/planos/subscrições).
- P2: Automações (Email/WhatsApp/SMS, lembretes); Marketing (landing pages, blog, SEO, newsletter); Website Builder (portfólio).
- P3: QR Codes e downloads por resolução nas galerias.
- Contas a Pagar (NÃO pedido ainda — não iniciar sem pedido).

## Dívida técnica / notas
- `GET /api/clients` vaza `password_hash=""` via `response_model=Client` (pré-existente, fora de escopo).
- Pagamentos/downloads ainda mockados até integração Stripe.

## Credenciais de teste
Ver `/app/memory/test_credentials.md`. App do fotógrafo sem auth; Portal cliente: ana.rui@email.pt / cliente123.
