# StudioHub AI — frontend
# Esta variável é lida em tempo de BUILD (craco/CRA), não em runtime.
# É passada ao Docker build como --build-arg REACT_APP_BACKEND_URL=...
# (ver docker-compose.prod.yml). Este ficheiro serve apenas de referência.

REACT_APP_BACKEND_URL=https://app.oteudominio.pt
